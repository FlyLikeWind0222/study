1.import 引入包（C、C++是include）
         eg: import java.lang

2.包相当于文件夹 包下面的类相当于文件夹下的文件

3.第一段说明方法可以做什么事。

4. int a=B.add(); 左边返回int型的值，右边执行加法操作

5.方法摘要里的右边就是成员方法

6.有static修饰的方法可以用 类名.方法来调用。 

7.没有static修饰的方法用变量.方法来调用。
  eg: String abc="123" abc.endsWith(str)

8.60度 cos(60/180*Math.PI)
  返回60度的余弦 Math.cos(60/180*Math.PI)

9.String a = "www.baid.com/sdasdasd"
  a.indexOf("www.baidu.com") != -1

10.截取小数点后面的 String a = "abc.edf"
   int index = a.indexOf('.');找到索引
   String sub = a.subString(index)

11.把字符串转成字符
  arr.join(",")
  "1,2,3"
  join("")
  join("\n")
  split(",")

12.看是什么数据类型 找到相应的类 再找到成员方法



  